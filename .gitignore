node_modules/
dashboard/node_modules/
dashboard/.next/
.scratch/
*.log
*.tsbuildinfo
.DS_Store

# Campaign lists and exclusion rules ship, because they are the demo's input and carry
# no personal data — company names, categories, public domains. A list with named
# contacts does not belong in git.
campaigns/*/*-private.csv

# Run output
/artifacts/

# Local preview config
.claude/
